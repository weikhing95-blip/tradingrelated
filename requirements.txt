anthropic>=0.69
pydantic>=2
pypdf>=4  # only for extracting memo text from a PDF (extract_belief.py step)

# Mag 7 News Bot (mag7bot/)
python-telegram-bot[job-queue]>=21  # async bot + JobQueue (polling + daily digest)
httpx>=0.27  # async HTTP for source polling (also a python-telegram-bot dep)
